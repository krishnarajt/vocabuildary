apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vocabuildary

resources:
  - 00-namespace.yaml
  - 10-externalsecret.yaml
  - 30-deployment.yaml

commonLabels:
  app.kubernetes.io/name: vocabuildary
  app.kubernetes.io/part-of: krishnarajt-homelab
