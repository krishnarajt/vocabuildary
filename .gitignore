# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.venv/
venv/
env/
build/
dist/

# uv
# (keep uv.lock tracked once we have one; for now it's optional)

# Env / local secrets
.env
.env.local
.env.*.local

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Logs
logs/
*.log

# SQLite (legacy v1 artifact — we're on Postgres now but keep just in case)
*.db
vocab.db

# Docker
.dockerignore.local
